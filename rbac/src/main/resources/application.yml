server:
  port: 8004

spring:
  application:
    name: rbac-service

# 注册中心
eureka:
  client:
    #register-with-eureka: false
    #fetch-registry: false
    service-url:
      defaultZone: http://127.0.0.1:8001/eureka/
    instance:
      prefer-ip-address: true
      instance-id: rbac-service:127.0.0.1:8004